Rows use the panel's tint at full strength; the panel behind them is the soft version. Emoji are user content (a grocery item's food emoji, a country flag) — never decorate UI chrome with them.

```jsx
<ListRow label="Apples" emoji="🍎" tone="sand" onToggle={t} />
```
