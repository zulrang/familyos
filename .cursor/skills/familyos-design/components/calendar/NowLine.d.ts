/** 2px coral current-time line with a dot at its left end; spans only today's column. */
export interface NowLineProps { top?: number; style?: React.CSSProperties }
export declare function NowLine(props: NowLineProps): JSX.Element;
