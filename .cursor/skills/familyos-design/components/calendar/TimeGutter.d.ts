/** Hour labels down the left edge of the calendar grid, aligned to each hour line. */
export interface TimeGutterProps { hours?: string[]; rowHeight?: number; width?: number; style?: React.CSSProperties }
export declare function TimeGutter(props: TimeGutterProps): JSX.Element;
