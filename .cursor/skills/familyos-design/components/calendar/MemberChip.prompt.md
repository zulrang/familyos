Row of these sits under the header, one per member, each flexing to equal width.

```jsx
<MemberChip name="Dad" tone="teal" count="1/20" />
```

Deselected chips drop to the sunken grey with 50% opacity.
