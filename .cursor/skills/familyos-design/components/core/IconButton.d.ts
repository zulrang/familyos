/** Round, icon-only control — calendar prev/next arrows and other compact affordances. */
export interface IconButtonProps { icon: string; size?: number; label: string; onClick?: () => void; style?: React.CSSProperties }
export declare function IconButton(props: IconButtonProps): JSX.Element;
