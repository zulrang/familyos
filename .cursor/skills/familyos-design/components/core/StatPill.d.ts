/** Tiny icon+number pill on a member's task column header: chores done (check) and reward points (star). */
export interface StatPillProps { icon?: string; value?: React.ReactNode; tone?: 'teal'|'blush'|'lilac'|'sage'|'coral'|'sand'; style?: React.CSSProperties }
export declare function StatPill(props: StatPillProps): JSX.Element;
