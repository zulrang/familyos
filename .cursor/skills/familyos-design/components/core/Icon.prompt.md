Wraps a Lucide outline glyph; use it anywhere FamilyOS shows an icon (nav rail, buttons, tabs).

```jsx
<Icon name="calendar" size={22} />
```

Icons are 1.5–2px stroke Lucide outlines, never filled and never emoji (emoji appear only inside user list content). Colour comes from `currentColor`, so set colour on the parent.
