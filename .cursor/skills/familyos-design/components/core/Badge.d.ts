/** Round count/date badge — the coral "18" on today's date, list item counts, "+2" avatar overflow. */
export interface BadgeProps { children?: React.ReactNode; tone?: 'coral'|'amber'|'teal'|'lilac'|'neutral'|'quiet'; size?: number; style?: React.CSSProperties }
export declare function Badge(props: BadgeProps): JSX.Element;
