# One Household server with paired Displays

One local Server Installation represents exactly one Household and serves any
number of paired browser-profile Displays. Household data and provider
connections are shared on the server, while only UI scale varies by Display;
pairing and revocation protect the household without introducing member
accounts or trusting every client on the LAN.
